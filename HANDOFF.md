# HANDOFF — 법률 절차 길잡이 MCP

- **마지막 갱신**: 2026-09-01 22:25 · 클로드코드
- **지금 하는 일**: 8/31 개발 완료 마감 **끝남**(제출 상태 완성). 그 뒤 ORCA 법률 검수를 돌려 **고쳐야 할 것 7건**을 확인했다. **다음 세션의 첫 일감이 이것이다.**

## 🔴 내일 아침 첫 일감 — 재배포가 f83e9b7에서 멈춰 있다

9/1 18:49 이후 커밋 4개가 라이브에 안 올라갔다. 19시·20시에 두 번 재배포를 눌렀는데
라이브는 그대로 `f83e9b7`이다.

**안 올라간 것**: `62b2267`(서식 4건 — 부당해고 각하 경고·임차권등기 2023 개정·제출처)
· `6bdd10a`(빈칸 회색 예시) · `dbf04bc`(**서버 안내문 분야 목록** — 호출률에 영향)
· `c09cf52`·`3cdbb07`·`5dddce8`(문서)

**배포 확인된 뒤 할 것**: 콘솔 MCP 설명을 `docs/CONSOLE_META.md`의 9/1 개정본으로 교체.
주제 274→279·법령 277→321이 반영돼 있고, 발화 예시와 공권력 계열을 앞세웠다.
⛔ 「등록 및 심사 요청」은 여전히 누르지 말 것.

**코드 문제는 아니라는 것까지 확인했다.**
- GitHub origin/main = 로컬 = `3cdbb07` (일치)
- 미커밋·미추적 파일 0
- 깨끗한 클론에서 `npm ci` + `npm run build` 둘 다 성공

→ 콘솔 쪽이다. 확인할 것 셋:
1. 배포 이력에 19~20시 배포가 **실패**로 찍혀 있는지
2. 소스 커밋이 `f83e9b7`로 **고정**돼 있는지 (최신으로 다시 잡아야 함)
3. 「빌드」가 배포와 **별도 단계**인지 (배포만 누르면 옛 이미지가 다시 올라간다)

반영 확인 한 줄:
```
curl -s -X POST https://legal-navigator-kakaotools.playmcp-endpoint.kakaocloud.io/mcp \
  -H 'content-type: application/json' -H 'accept: application/json, text/event-stream' \
  -d '{"jsonrpc":"2.0","id":1,"method":"tools/call","params":{"name":"get_form_template","arguments":{"key":"부당해고구제신청서"}}}' | grep -c 각하
```
1이 나오면 반영된 것이다.

⚠️ 팀에는 "서식 수정분은 배포가 밀려서 내일 올라간다"고 공지했다(9/1 디스코드).


## ✅ 마감 시점 상태 (2026-08-31 23:30 실측)

라이브 `legal-navigator-kakaotools` = 최신 main. 재배포·콘솔 저장 모두 완료했다.

- 툴 **16** · 주제 **274** · 서식 **121** · 법령 **277** · 판례 194 · 테스트 **229**
- 5인 미만 오안내 교정 라이브 확인: `"5인 미만 사업장인데 부당해고 당했어요"` → **"5인 미만 사업장 — 되는 것과 안 되는 것"**
- 콘솔: 심사 전(임시등록) · 설명 451자(274주제 반영) · 대화예시 3개 · 소개문구 17자 · 소개 이미지 5장

**재배포는 즉시 반영이 아니다.** 누른 직후엔 직전 커밋이 떠 있고 수십 초~수 분 뒤 바뀐다(8/26·8/31 모두 그랬다). 검증은 시간을 두고 다시 할 것. 확인 한 줄:
```
curl -s -X POST https://legal-navigator-kakaotools.playmcp-endpoint.kakaocloud.io/mcp \
  -H 'content-type: application/json' -H 'accept: application/json, text/event-stream' \
  -d '{"jsonrpc":"2.0","id":1,"method":"tools/call","params":{"name":"triage","arguments":{"situation":"5인 미만 사업장인데 부당해고 당했어요"}}}'
```

## 어디까지 했나 (사실만)

### 제출 상태 — 마감 충족
- 콘솔: 등록상태 **심사 전(임시등록)** · Online · Tools 16 · Endpoint `legal-navigator-kakaotools` · 인증 미사용
- 메타정보 저장 완료: 설명 412/500 · 대화예시 3개(월급/전세/유튜브 가짜뉴스) · 소개문구 「법을 몰라도, 절차부터 서식까지」(17자) · **소개 이미지 5장 업로드**
- 규모: **주제 274 · 서식 121 · 법령 277 · 판례 194 · 테스트 229**

### 8/31 저녁 — 마감 대응 (라이브 반영됨)
- `01ddee4` **for_assistant 복구**. 코덱스 `5e1ecbc`가 "가이드에 없는 필드"라며 전부 지웠는데, 그게 서식 본문을 호스트 LLM에 넘기는 통로다. 없으면 카드는 떠도 모델이 서식 내용을 몰라 **지어낸다**(라이브에서 응답 1,504자·본문 0자 상태였음 → 복구 후 2,854자).
- `d15fa62` 입력칸 너비 ch→em (ch는 숫자 0 폭 기준이라 한글을 절반으로 잼) + 카카오 렌더러가 배지 Row를 줄바꿈하지 않아 판례 배지가 잘리던 것(배지 축약, 기한·관할은 본문 줄로).
- `b7027dc` **서식 글자 겹침의 진짜 원인** — `.ln-h{text-indent:-1.5em}`이 상속 속성이라 inline-block 입력칸 안까지 내려가 글자가 22.5px 왼쪽에 그려짐. `.fld,.cbx{text-indent:0}`으로 차단. 전역 CSS라 121종 전부 적용.
- `b052f81`·`a617e49` 소개 이미지 5장 제작(960×960·투명배경·폰 목업·Pretendard 규격).

### 8/31 밤 — 팀 GAP 6개 (라이브 반영 완료)
`7accca4` + `226925c`. 팀이 `GAP_ANALYSIS.md`에 정리한 보완 6개를 전부 반영했다.

- **오인미만사업장** (1순위, 🔴 오안내 교정) — "5인 미만인데 부당해고"가 부당해고 구제 절차로 가고 있었다. 근로기준법 시행령 별표 1을 법제처 원문으로 확인한 결과 상시 4명 이하에는 제23조가 **제2항만** 적용되고 **제28조(노동위 구제신청)는 목록에 없다.** 노동위에 가면 각하된다.
  주제만 만들어서는 안 고쳐졌다 — '부당해고'가 제목에 정확히 걸려 계속 1등이었다. **규모 신호(5인 미만·상시 근로자 4명·직원이 나 혼자 등)를 잡으면 규모 주제를 최고점 위로 올리는 규칙**을 `rankTopics`에 넣었다.
- **근로계약서미작성** (2순위) — 제17조①②, 제114조(500만원 이하 벌금)
- **주휴수당미지급** (6순위) — 제55조①. 계산기는 `calc.ts`에 이미 있었는데 발화가 닿을 주제가 없어 죽어 있던 것을 이어줬다
- **프리랜서근로자성** (4순위) — 계약 형식이 아니라 실질로 판단. 인정 사례(2021두33715 위탁계약형 지점장)와 부정 사례(2020다296819 채권추심원)를 한 쌍으로 실어 **단정하지 않는다**
- **연대보증채무대응** (5순위) — 종전에 개인회생으로 가던 것. 민법 제437조 단서(연대보증인은 최고·검색의 항변 불가)를 축으로 보증인 보호법 제4·5·7·11조를 엮음. 지급명령 2주 이의가 가장 급하다는 점을 앞에 세움
- **자전거킥보드사고** (6순위) — 도로교통법 제2조 21의2가 자전거·PM을 '자전거등'으로 두어 '차'로 다룬다. 제54조 조치의무·제148조(5년 이하 징역) 적용. "자전거니까 그냥 가도 된다"가 뺑소니가 되는 지점

함께 고친 것:
- 새 주제 근거법 15건이 `STATUTES`에 없어 `get_statute`·`verify_citation`이 모르던 것을 등록(262→277). 전부 법제처 실존 확인.
- audit 검증기가 `제44조의10제3항`처럼 항까지 적힌 표기를 파싱 못 해 MISMATCH 3건을 내고 있었다. 조 단위로 잘라 조회·대조하도록 수정 → **법령 277건 실존확인 277 · 부재 0 · 불일치 0**
- 팀이 "판례 10건 미검증"으로 본 것은 **미검증이 아니라 중복**이었다. 194건 중 10건은 같은 사건번호를 두 주제에 인용한 것(2005다4529 등)이고, 고유 사건번호 184건은 이미 전수 대조를 마쳤다.

## 🆕 2026-09-01 14:48 카카오 공지 — 실기기 테스트 개통 (9/1 ~ 9/10 11:00)

Arin(PlayMCP)이 본선용 Endpoint로 등록된 우리 MCP를 **제작자 계정의 Kakao Tools MY에 직접 담아줬다.**
프리뷰(preview-chatgpt.kakao.com)가 아니라 **실제 카카오톡 앱**에서 대화하며 쓸 수 있다.

- 위치: 카카오톡 → 대화 → ChatGPT → 우측 메뉴 `Kakao Tools` → `MY`
- 대상: 8/31 기준 팀프로필 멤버 중 **Kakao Tools 가입자만**(미가입자는 권한 없음, 인원 추가·수정 불가)
- 기간: 9/1(월) ~ **9/10(수) 11:00** — 이후 MY에서 일괄 제거됨
- `일시적으로 이용할 수 없습니다`가 떠도 **툴콜은 정상**이다(카카오가 명시). 장애로 오판하지 말 것.

### ⛔ 새로 생긴 금지선 2개
- ⛔ **MY에서 서비스를 '제거'하지 말 것 — 재추가가 불가능하다.** 실수로 누르면 9/10까지 실기기 테스트를 못 한다.
- ⛔ **캡처·대화내역을 외부에 유출하지 말 것.** 정식 서비스 이전의 선공개 테스트다.
  디스코드 팀 채널 안은 되지만, 웹사이트·SNS·공모전 홍보물·포트폴리오에 쓰면 안 된다.
  (소개 이미지 5장은 카카오 콘솔에 올린 것이라 무관하다.)

### 이 공지로 바뀌는 일정
**9/10 11:00이 실질적인 실기기 검증 마감**이다. 9/14 코드 프리징보다 나흘 앞선다.
이슈는 기간 내에 수정·공유하라고 했으므로, 발견 → 수정 → 재배포 사이클을 9/10 전에 돌려야 한다.

## ✅ 2026-09-01 저녁 추가 — 서식 121종 점검 (내용 45종 + 화면 전수)

팀원 두 분이 8/6부터 서식 리뷰를 못 하고 있어(1/114) 은미님이 직접 하기로 했고,
121종을 손으로 보는 대신 **자동 검사 + 이미지 훑기 + 내용 정독**으로 쪼갰다.

### 도구
- `scripts/capture-forms.sh` — 121종을 PNG로 뽑는다(헤드리스 크롬, DPR2, 900×2400).
  브라우저를 121번 여는 대신 이미지 폴더를 넘기며 겹침·잘림만 본다. 출력 기본값은
  `~/Downloads/서식리뷰_MMDD`.
- 같은 폴더에 `_점검.html`(키보드 뷰어)을 만들어 썼다 — J/K로 넘기고 X로 표시,
  C로 목록 복사, localStorage에 표시 보존. 파인더는 파일을 하나씩만 미리보기 하므로
  화살표로 넘어가지 않는다(그게 은미님이 막힌 지점이었다).

### 자동 검사로 걸러진 것 — 거의 없음
작성요령 누락 0 · 용도 부실 0 · 빈칸 0개 서식 0 · 본문 잘림 0.
제출처 언급 없음 2건뿐. **데이터는 멀쩡했고, 사람 눈이 필요한 건 화면과 문구였다.**
※ 오탐이었던 것: 민감번호 56건(소장의 `(주민등록번호)`는 표준 항목, `[은행/계좌번호]`는
받을 계좌라 있어야 한다) / 괄호 짝 3건(①②③ 기호) / 주민번호 안내 3-121건
(서식별 작성요령이 아니라 **전역 `작성지침()`과 미리보기 배너에 이미 있었다**).

### 내용 검수 45종 → 4건 수정 (`62b2267`)
1순위 45종 = 노동·주택임대차·돈거래·형사·민사·교통·가정폭력·스토킹·소비자·금융사기.
- 🔴 **부당해고구제신청서** — 본문에 '상시 근로자 수' 칸은 있는데 4명 이하면
  신청 자체가 각하된다는 경고가 없었다. **주제는 그날 고쳤는데 서식이 남아 있었다.**
  기한(3개월)보다 먼저 확인하도록 순서를 바꿨다. ⛔ 이 경고를 지우지 말 것.
- 🟠 **임차권등기명령신청서** — 2023.4.18 개정 미반영. 주임법 제3조의3③이 민사집행법
  제292조를 준용하게 되어 ①임대인 송달 전에도 집행되고(제292조③) ②임차인이 고지받은
  날부터 2주가 지나면 집행할 수 없다(제292조②). 전세 피해자에게 결정적인 두 줄이다.
- 🟡 제출처 없던 신고·진정 4건 — 임금체불진정서(지방고용노동청)·부당해고구제신청서
  (지방노동위원회)·스토킹피해신고서(관할 경찰서 여청과)·학교폭력신고서(학교·117).

### 화면 점검 121종 → 1건 수정 (`6bdd10a`)
겹침·잘림은 **없었다**(오전 `.fld{text-indent:0}` 수정이 유효). 대신 은미님이
**62종에서 빈칸 회색 예시가 안 뜨는 것**을 찾았다.
원인은 데이터가 아니라 렌더러였다 — `[성명]`처럼 이름 붙은 빈칸은 그 이름을 `data-ph`로
쓰는데 `[______]`처럼 밑줄만 있는 빈칸은 ph를 빈 문자열로 뒀다.
데이터 62종을 고치는 대신 **`앞항목명()`을 새로 만들어 빈칸 바로 앞의 항목명을 끌어온다.**
작성일 자리(`20[__]. [__]. [__].`)는 앞말이 "20"·"."뿐이라 별도 정규식으로 연·월·일을 못 박았다.
결과: 예시가 비어 있던 칸 **308/2006(15.4%) → 105(5.2%)**. 남은 105는 별지·표처럼
앞에 항목명이 실제로 없는 자리다.

### 2순위 76종 — 중복 2쌍 발견·정리 (`후속 커밋`)
전부 읽지는 않았고 **같은 주제에 서식이 2개 이상 붙은 경우**를 먼저 훑었다. 6건 중 4건은
정상(임금체불=진정서·내용증명·대지급금처럼 서로 다른 문서)이었고, 2건이 진짜 중복이었다.
- `행정심판청구서`(예시본) vs `행정심판_청구서`(행정심판법 시행규칙 별지 제30호)
- `정보공개청구서`(예시본) vs `정보공개_청구서`(정보공개법 시행규칙 별지 제1호의2)
어느 쪽이 나갈지 호스트 LLM이 정하는 구조라 **더 나쁜 쪽이 나갈 수 있었다.**
공식 별지본만 남기고 예시본에만 있던 항목은 옮겼다 — 「처분이 있었던 날」(180일 기산점),
「이의신청 30일」(정보공개법 제18조). 조문 확인하면서 행정심판법 제27조④~⑦(불변기간,
기간을 잘못 알렸거나 안 알린 경우, 무효확인심판은 기간 제한 없음)도 함께 넣었다.
**서식 121 → 119.** 중복 재발 방지 테스트 추가.

### 남은 것
- 2순위 76종의 본문 정독은 아직이다. QA 지적이 오면 그때 본다.
- 수신처 없는 관공서 서식 23건 중 4건만 고쳤다. 나머지 19건은 작성요령·온라인접수에
  제출처가 적혀 있어 급하지 않다.

## ✅ 2026-09-01 완료분 — 법률 검수 7건 + 라우팅 12건

### 오전: ORCA 법률 검수 7건 전부 반영 (`95013a7`)
법제처 원문으로 재확인 후 반영했다. 오탐 0건. 보고서 `audit/REVIEW_RESULT_0901_A.md`.
- 특가법 제5조의3 — 전동킥보드는 '원동기장치자전거'라 도주 시 **1년 이상** 유기징역.
  종전엔 도교법 제148조 '5년 이하'만 실어 상·하한이 뒤바뀌어 보였다. 자전거는 대상 아님.
- 보증인보호법 제2조1호 가~바목 배제 — 회사 대표의 배우자·직계존비속·동업자 보증은 이 법 대상이 아니다.
  그때 살아 있는 민법 제428조의2(서면)·제428조의3②(근보증 최고액)를 근거법에 추가.
- 5인 미만 '안 되는 것' — 별표 1에 제46(휴업수당)·제56(가산수당)·제60조(연차)가 없다.
  8/31에는 노동위 불가만 잡고 수당을 빠뜨려 "임금은 그대로 청구 가능"이 가산수당까지 되는 것처럼 읽혔다.
- 제26조 단서 1호(계속 근로 3개월 미만이면 해고예고수당 없음).
- 주휴 15시간은 제18조③ "4주 평균" / 개근 근거 시행령 제30조① / 계산 근거 별표 2.
- 프리랜서 주제의 5인 미만 함정(근로자성 인정돼도 4명 이하면 각하).
- 교특법 제3조② 12개 예외(킥보드는 횡단보도·보도 침범이 전형).
법령 **277 → 285**. 전수 대조 실존확인 285 · 부재 0 · 불일치 0.

### 오후: 라우팅 12건 (`bad147c`, `9e9e281`)
ORCA 발화 테스트(48발화·55호출, `audit/UTTERANCE_TEST_0901.md`)와 자체 점검이 같은 곳을 짚었다.
동의어가 **글자 그대로 일치할 때만** 걸리는 구조라 활용형 하나가 어긋나면 통째로 0건이 됐다.
- 🔴 규모 신호 5건이 전부 **노동위 각하 경로**로 갔다. '직원 세 명'은 있는데 '직원 3명'이 없고,
  '직원이 나 혼자'는 있는데 '저 혼자'가 없고, "직원이 **나 포함** 세 명"처럼 사이에 말이 끼면 못 잡았다.
  규칙 자체도 `score.has(...)` 조건이라 후보에 없으면 아무 일도 안 했다 → 해고·수당 맥락이면 밀어 넣도록 바꿈.
- 못 찾음 4건: '서류'(계약서 아님)·'주말에 일한 돈'(주휴 모르는 사람의 말)·'개인사업자로 **등록**'·'보증 **서줬**'.
- 순서 뒤집힘 2건: 프리랜서+퇴직금 → 근로자성이 먼저(진정 넣으면 '근로자 아니다'에서 막힘) /
  "교통사고가 났어요" → 과실비율 심의가 아니라 직후 처리.
결과: D 8/8 · A 6/6 · B 5/5 · C 16/16. 테스트 **230** 통과.
안 고친 것: "보증 선 것 때문에 지급명령이 날아왔어요" → 지급명령(2주 이의가 실제로 가장 급해 틀리지 않음).

### ⚠️ 실기기에서 확인된 것 — 툴이 자동으로 붙지 않는다
카카오톡 ChatGPT에서 그냥 물으면 **우리 툴이 호출되지 않고** 호스트 LLM이 자기 지식·웹검색으로 답한다.
입력창의 `⭕ Kakao Tools`를 눌러 「법률 절차 길잡이」를 선택해야 카드가 뜬다(4:42 확인).
그런데 **같은 대화방에서 몇 턴 뒤 선택이 풀렸다**(5:16·5:17 확인 — 칩이 사라지고 카드 없음).
→ 우리가 코드로 못 고치는 영역. 팀 채널에 정상 동작인지 문의할 것. 은미님이 재현 중.
참고로 툴 없이 답한 화면들은 **우리가 오전에 고친 오류를 그대로 냈다**(주휴 "1주 15시간",
5인 미만인데 부당해고 검토 권유, 서식은 지어냄) — 본선 설명 문안의 근거로 쓸 수 있다.

## 🔴 다음 세션 첫 일감 — 이미 끝남(아래는 기록용)

2026-09-01 새벽 ORCA(Claude)로 새 주제 6종의 A항목(법률 내용)을 검수했다.
보고서 전문: `audit/REVIEW_RESULT_0901_A.md`

**검수 품질이 높다. 오탐 0건이었고, 지적 전부를 클로드코드가 법제처 원문으로 재확인해 사실로 판정했다.**
확인 안 되는 것은 스스로 "확인 필요"로 남겼다(지각·조퇴 개근 여부, 해고예고수당 시효 등).

### 🔴 반드시 고칠 것 4건

1. **특가법 제5조의3 누락** — `traffic.ts:9` · **가장 시급**
   전동킥보드는 도교법 제2조19의2가 "원동기장치자전거 중"으로 정의 → 특가법 '자동차등'에 포함.
   사람을 다치게 하고 도주하면 **제5조의3①2호 "1년 이상의 유기징역"**(원문 확인).
   지금은 도교법 제148조 "**5년 이하**"만 안내 → 이하↔이상은 집행유예냐 실형이냐를 가른다.
   ⚠️ **자전거는 원동기장치자전거가 아니라 특가법 미적용** → 두 기기를 한 주제에 묶은 구조 자체를 나눠야 한다.

2. **보증인보호법 적용 배제 미안내** — `money.ts:9`
   제2조1호 **가~바목 6개**가 제외 대상(원문 확인): 사업 관련 보증(가), 법인 대표·이사·과점주주의 회사채무 보증(나),
   **그 배우자·직계존비속(다)**, 동업자 보증(라), 채무인수 기업 보증(마), 신용보증기금 등(바).
   현실 연대보증 피해의 다수가 여기 해당하는데, 단계 3~5(최고액 특정·3년 간주·통지의무)가 통째로 적용되지 않는다.
   → 배제 경고 + **민법 제428조의2(보증은 서면이어야 효력)·제428조의3②(근보증 최고액 미특정 시 무효)**를 근거법에 추가.
   이 둘은 민법이라 배제 케이스에서도 살아 있는 유일한 무기다.
   ※ 검수는 가·나·라만 짚었으나 클로드코드 확인 결과 **다목(배우자·직계존비속)도 배제**다.

3. **해고예고 3개월 예외 누락** — `labor.ts:74`
   제26조 단서 1호(원문 확인) — *"근로자가 계속 근로한 기간이 **3개월 미만**인 경우"*는 해고예고수당 자체가 없다.
   5인 미만 사업장은 단기 알바 비중이 커서 이 예외에 걸리는 사람이 많은데, 지금 문장은 무조건 받는 것처럼 읽힌다.

4. **5인 미만 "안 되는 것"의 절반 누락** — `labor.ts:74`
   별표 1 조문 번호를 전수 추출해 확인: **제46조(휴업수당)·제56조(연장·야간·휴일 가산수당)·제60조(연차) 없음.**
   (별표 1 수록: 1·13·15·17·18·19·20·22·23·26·42·43·45·47·54·55·63·64·65·66·69·70·71·72·74·76·78·92·101·106·107·116)
   5인 미만 문의 최다 유형이 "야근수당을 안 준다"인데 지금은 "임금은 그대로 청구 가능"으로만 안내 → 가산수당까지 된다고 오해한다.
   ⚠️ 8/31에 클로드코드가 "5인 미만 오안내를 잡았다"고 했으나 **절반만 잡은 것이었다.**

### 🟠 보완 3건

5. **주휴수당 "1주 15시간" → "4주 평균"** — `labor.ts:122`
   제18조③ 원문 — *"**4주 동안**(4주 미만이면 그 기간)을 **평균하여** 1주 소정근로시간이 15시간 미만"*.
   주마다 시간이 들쭉날쭉한 알바는 결론이 뒤집힌다. **제18조③이 근거법에 없다**(15시간의 유일한 출처).
   개근 요건 출처도 없음 → **시행령 제30조①** 추가. 계산식 근거는 **시행령 별표 2** 제2호 나목·제4호 라목.
   비고의 "5인 미만도 그대로 적용"은 **제55조①만** 해당(② 관공서 공휴일은 별표 1에 없음).

6. **프리랜서 단계 5에 5인미만 함정 재발** — `labor.ts:146`
   단계 5가 노동위 구제신청을 안내하는데, 근로자성이 인정돼도 **5인 미만이면 각하**된다.
   오인미만사업장 주제를 만든 바로 그 이유가 옆 주제에 그대로 남아 있다. "상시 5명 이상인지 먼저 확인" 한 줄 필요.
   비고의 3.3% 서술도 확장 권장 — 2021두33715는 **4대보험 미가입·취업규칙 미적용·기본급 없음**까지
   "그것만으로 근로자가 아니라고 쉽게 단정해서는 안 된다"고 판시했다.

7. **교통사고처리 특례법 누락** — `traffic.ts:9`
   교특법 제2조1호가 도교법 제2조17호가목 '차'를 그대로 받아 **자전거·PM 포함**.
   제3조② — 합의하면 원칙적으로 기소되지 않으나 **12개 예외**(특히 횡단보도 보행자 보호의무 위반·보도 침범)는 합의해도 기소.
   킥보드 사고의 전형이 보도 주행·횡단보도다. 이걸 넣으면 단계 마지막의 "자동차 사고와 같은 기준"이 근거 있는 서술이 된다.

### 🟡 조문으로 확인 안 된 것 (지어내지 말 것)
- 해고예고수당의 소멸시효 3년 — 근거 조문 없음(임금 3년·퇴직금 3년은 확인됨)
- "지각·조퇴는 개근을 깨지 않는다" — 시행령 제30조①은 "개근"이라고만 함. 행정해석 영역
- 보증채무 시효가 주채무를 따라간다 — 조문 없음. 판례 법리 영역
- 일상생활배상책임보험이 교특법 제4조 요건을 채우는지 — 조문만으로 판단 불가

**반영 순서 권장**: 1 → 2 → 3·4(같은 파일) → 6 → 5 → 7
반영 후 `npm test` → 스냅샷 갱신 → push → 재배포 → 라이브 확인.

## 다음에 할 일

1. **9/1~9/11 카카오 QA 감시** — 팀별 디스코드. 지적이 오면 **해당 영역만** 수정 → `npm test`(229) → push → 재배포 → 시간 두고 라이브 재확인.
2. 프리뷰에서 서식 초안 흐름 재확인(for_assistant 복구 후 미검증): `임금체불 진정서 양식 보여줘` → `제 이름 홍길동이고 3개월치 300만원 못 받았어요. 채워서 보여줘`. 채운 초안이 코드블록으로 **먼저** 나오는지. 안 나오면 `src/server.ts`의 `작성지침` 첫 두 불릿 조정.
3. 서식 겹침 최종 확인 — `/forms/임금지급_내용증명`의 `2. 본인은 [입사일]부터…` 줄 + 다른 서식 무작위 몇 개. (제작자 브라우저는 한글 폰트 폭을 잘못 재는 환경이라 실제 크롬 확인이 마침표)
4. ListView 검증(선택) — `LISTVIEW=on`이면 find_legal_aid가 ListView로 나감(현재 off). 프리뷰에서 목록 렌더 + 줄 눌러 전화가 되면 기본값 전환 검토. `src/server.ts` `listViewOn()`.
5. **9/14 코드 프리징** 전 최종 태그. 이후 수정 금지.

## 팀원 검수 요청 (용우·예은)

⚠️ **8/24 main(`621d408`)을 보고 검수했다.** 이후 17개 커밋이 있으니 `git pull` 먼저.

- **법률 내용 검수(최우선)** — 새 주제 6종. 특히 `오인미만사업장`의 "노동위 구제 불가 / 해고예고수당·임금·퇴직금은 가능" 구분. 틀리면 이용자가 손해를 본다.
- **발화 테스트** — 새 6개가 도달하는지 + 기존(부당해고·임금체불·교통사고·대여금·퇴직금)을 뺏지 않는지. 후자는 회귀 테스트로 고정해뒀지만 실제 발화로도 확인 권장.
- **서식 리뷰 1/114** — 8/6부터 열린 건. QA 기간이 기회.

## 막힌 것 / 결정 대기

- 없음. (ListView 기본값 전환만 프리뷰 결과 보고 은미님 결정)

## 손대면 안 되는 것

- ⛔ **「등록 및 심사 요청」 버튼** — 누르면 거절 처리(공지 명시). 임시등록 유지. 콘솔에서 하얀 버튼으로 맨 왼쪽에 있어 기본 버튼처럼 보인다.
- ⛔ **예선 서버 `legal-navigator-full`** — 삭제·중지·재배포 금지, 운영 상태 유지. (코덱스가 한때 이 서버를 재배포하라고 잘못 안내한 적 있음)
- ⛔ **`for_assistant` 필드 제거 금지** — 카카오 가이드에 없는 필드지만 8/26 프리뷰에서 카드 정상 렌더를 실물 확인했다. 지우면 서식 초안 기능이 죽는다. 봉투 검사 테스트에 근거 주석 있음.
- ⛔ **`.fld{text-indent:0}` 제거 금지** — 지우면 서식 글자 겹침이 되살아난다.
- ⛔ **Kakao Tools MY에서 우리 서비스를 '제거' 금지** — 재추가 불가(9/1 카카오 공지).
- ⛔ **실기기 테스트 캡처·대화내역의 외부 유출 금지** — 선공개 테스트다. 팀 디스코드 밖으로 내보내지 말 것.
- ⛔ 주민등록번호 등 민감번호 6종을 채팅으로 받는 코드 금지(개발가이드 §5.3).
- 9/16 전 "카카오톡에서 써보세요" 홍보 금지. 계약 제9조: 공모전 언급은 "본선 진출작"까지만.

## 관련 파일·링크

- 레포: `~/Projects/legal-navigator-mcp` (github.com/yuneunmi814-cmyk/legal-navigator-mcp, main)
- 팀 시트: `ka_mcp_법률절차길잡이`(용우님 소유) — 8/31 작업 T-09·T-10·T-11로 반영 완료
- 마감 체크리스트: `KAKAO_TOOLS_FINAL.md` (⚠️ 소개문구·대화예시는 콘솔 저장본이 최신 — 이 인계장 기준으로 볼 것)
- 팀 GAP 분석: `GAP_ANALYSIS.md` (6개 전부 반영 완료 — 문서는 아직 미반영 상태로 남아 있음)
- 검증 리포트: `audit/REPORT.md` · 재생성 `npx tsx scripts/verify-statutes.ts --statutes|--precedents`
- 콘솔 playmcp.kakao.com/console · 프리뷰 preview-chatgpt.kakao.com (은미님 계정 필요)
- 명령: `npm test` · `npm run typecheck` · `npm run build` · 로컬 `WIDGETS=on PORT=4188 node dist/server.js`
- 위젯 미리보기: `/widgets/{form|triage|calc|aid|procedure|checklist|deadline}` (aid는 `?lv=1`로 ListView 비교)
- 소개 이미지 원본: `assets/kakao-tools-examples/` · 사본 `~/Downloads/카카오툴즈_소개이미지/`
