# HANDOFF — 법률 절차 길잡이 MCP

- **마지막 갱신**: 2026-09-04 04:10 · 클로드코드
- **지난 기록** → [`HANDOFF-archive.md`](HANDOFF-archive.md) (8/31~9/3 경과 전문. 본문 그대로 옮겨둠)
- **라이브 = main**. 최신 커밋 `057d389`
- ✅ **`802286f`(9/4 03:3x, 다른 세션)** — 아린님 확답("LLM이 보는 건 description뿐")에 따라
  **공권력 발화 예시를 입구 도구 description에 직접** 심었다. 9/1에 `SERVER_INSTRUCTIONS`
  분야 목록만 고친 것은 모델에 닿지 않았다는 판단. 테스트 동반.
  👉 **아직 재배포 안 됨. 라이브 확인 필요**(`/healthz` 또는 실기기).
  ⛔ 심은 예시 4개("병원에 입원시켰어" / "현행범" / "경찰서 잠깐 가자" / "공무원이 반말")를
  지우지 말 것 — 지우면 그 계열은 다시 호출 신호를 잃는다. 테스트로 고정해 뒀다.
- ✅ **소개 문구 17자는 바꾸지 않는다(9/4 은미님 결정).** 대안(「어디에 무엇을 언제까지
  내는지」 등)을 검토했으나 현행 「법을 몰라도, 절차부터 서식까지」 유지. **재론 금지.**

## 지금 상태 한눈에

| 항목 | 값 |
|---|---|
| 단계 | AGENTIC PLAYER 10 **본선 진출작**, 카카오 Kakao Tools에 등록(심사 전·임시등록) |
| 규모 | 툴 16 · 주제 279 · 서식 **119** · 법령 **321** · 판례 194 · 테스트 241 |
| ⏰ 실기기 테스트 종료 | **9/10(수) 11:00** — D-6 |
| ⏰ 코드 프리징 | **9/14(일)** — D-10. 이후 수정 금지 |
| 본선 공개 | 9/16 · 투표 ~10/12 |

**핵심 사실 하나**: 호출될지 말지는 **우리가 못 정한다.** 이용자가 칩을 켜도
호스트 LLM이 "내가 직접 답하는 게 낫다"고 보면 툴을 안 부른다. 카카오도 제어 못 한다
(9/3 아린님 확정). 그래서 **남은 승부처는 코드가 아니라 노출·안내다.**

```
이용자가 우리를 쓰기까지 관문 넷
① 추가 영역에서 찾는다 → ② MY에 담는다 → ③ 칩을 켠다 → ④ 그래도 호출은 LLM이 정함
```

## 다음에 할 일

1. **⏰ 실기기 발화 테스트 (은미님, 30분) — 9/10 전에 반드시.**
   9/3에 하기로 했는데 **결과가 기록돼 있지 않다(미확인).** 안 했으면 지금이 마지막 기회다.
   칩 켠 상태로 아래를 던지고 [넣은 문장 / 나온 카드 제목]만 적으면 된다.
   ```
   직원이 나 포함 세 명인데 갑자기 나오지 말래요    → 5인 미만 사업장
   친구 대출에 보증 서줬는데 은행에서 저한테 갚으래요 → 연대보증
   전동킥보드 타다 사람을 쳤어요                  → 1년 이상 유기징역이 보이는지
   경찰이 응급입원을 시켰어요                     → 본인 뜻과 달리 입원됐을 때
   임금체불 진정서 양식 보여줘                    → 카드가 뜨는지
   ```
   9/1에 하루를 들여 고친 것들이 실제로 나오는지 보는 것이 목적이다.

2. **본선 공개 준비 — 노출 문구 (9/16 전, 가장 값이 큰 일)**

   ★ **2026-09-04 PlayMCP 공개목록 실측으로 우선순위가 바뀌었다.**
   playmcp.kakao.com은 로그인 없이 전체 목록이 보인다(정렬: 추천/**툴콜**/최신).
   카드 구조 = [이름 / 제작자 / 설명 / Tools수 / 툴콜수].
   ⚠️ **17자 소개 문구는 이 화면에 안 나온다** — 그건 카카오톡 안 Kakao Tools 목록용(미확인).

   **툴콜 순 상위**: ①ArtBridge 15,465(개인) ②카카오톡 나챗방 8,563(Kakao)
   ③띵동-택배추적기 5,738(개인 신성수) ④스킨하모니 5,255(개인) ⑤카카오맵 4,729

   🔴 **개인 제작자 상위 3개가 전부 「이름 + 구분자 + 한 줄 가치」다.**
   - `ArtBridge:20만 공연데이터 1초만에 내취향으로`
   - `띵동 - 택배 추적기`
   - `스킨하모니 · 화장품 성분 신호등`

   이름만 쓰는 건 **카카오 공식뿐**(나챗방·카카오맵·멜론·톡캘린더). 브랜드가 있으니까.
   우리 「법률 절차 길잡이」는 브랜드 없는 개인인데 카카오 공식 스타일을 쓰고 있다.

   상위권 **설명 첫 문장은 전부 이용자의 순간**으로 연다
   (띵동 "…문자에 가슴 철렁하셨나요?" / 스킨하모니 "화장품 사기 전, 전성분만 붙여넣어 보세요").
   우리는 "…돕습니다"로 서비스 능력이 먼저다.

   ✅ **경쟁자 `Korean-law-mcp` 확인** — 판례 17만·현행법령 5,600을 내세우고
   "환각 방지"를 전면에 걸었는데 **툴콜 191**로 최하위권.
   8/26 포지셔닝("법령 DB 규모·환각 없는 AI로 붙으면 지는 싸움")이 실측으로 뒷받침됐다.
   ⛔ 그 방향으로 문구를 틀지 말 것.

   **해야 할 일, 값이 큰 순서**
   - ① ✅ **서비스명 변경 가능 확인(9/4 콘솔 실물)** — `MCP 이름` 칸 **최대 30자, 현재 9자**.
     👉 태그라인을 붙일 자리가 21자 있다.
     후보: `법률 절차 길잡이 · 되는 것도 안 되는 것도`(21) / `· 억울할 때 다음 한 걸음`(21)
   - ② 설명 398자의 **첫 문장**을 이용자의 순간으로 교체(나머지는 그대로 둔다)
   - ③ 소개 문구 재검토 — **20자 제한, 현재 17자라 세 글자 여유 있음**
     후보: 「억울할 때, 서식까지 손에」(14) / 「되는 것도, 안 되는 것도」(14) /
     「법을 몰라도 서식 119종까지」(16). 현재 문구는 '절차'가 이름과 겹친다.
   - ④ ★[[신성수]]님이 3위 제작자다(띵동). 물어볼 수 있는 사람이 실제 상위권에 있다.
   - 소개 이미지 5장에 "Kakao Tools에서 「법률 절차 길잡이」 선택" 안내가 있는지 확인
     ※ 카카오 Figma 템플릿 규격상 임의 텍스트 추가가 되는지부터 확인할 것(가이드 Don't: 그래픽 추가 금지)
   - 9/16 이후 쓸 홍보 문구 초안에 칩 켜는 법을 넣어 둔다

3. **카카오 QA 감시 (상시)** — 팀 디스코드. 지적이 오면 **수신 당일 분류**,
   해당 영역만 수정 → `npm test` → push → 재배포 → 시간 두고 라이브 재확인.

4. **9/14 프리징 전 최종 태그.**

5. (여유 있으면) 2순위 서식 76종 본문 정독 · 수신처 없는 관공서 서식 19건.
   둘 다 급하지 않다. QA 지적이 오면 그때 봐도 된다.

## 막힌 것 / 결정 대기

- **미확인 1**: 9/3 실기기 발화 테스트를 했는지. 결과가 어디에도 안 적혀 있다.
- ~~**미확인**: 서비스명 변경 가능 여부~~ → ✅ 9/4 확인, 30자까지 가능.
- **미확인 2**: 아린님이 말한 "description"이 셋 중 어느 것인지 특정 안 됨 —
  (a)도구 16개 각각의 description (b)`SERVER_INSTRUCTIONS` (c)콘솔 등록 설명 398자.
  문맥상 (a)로 보이나 확인 안 했다.
- ListView 기본값 전환(`LISTVIEW=on`)은 프리뷰 결과 보고 은미님 결정.

## 손대면 안 되는 것

- ⛔ **「등록 및 심사 요청」 버튼** — 누르면 거절 처리(공지 명시). 임시등록 유지.
  콘솔에서 하얀 버튼으로 맨 왼쪽에 있어 기본 버튼처럼 보인다.
- ⛔ **예선 서버 `legal-navigator-full`** — 삭제·중지·재배포 금지, 운영 상태 유지.
  (코덱스가 한때 이 서버를 재배포하라고 잘못 안내한 적 있다.)
- ⛔ **`for_assistant` 필드 제거 금지** — 가이드에 없는 필드지만 서식 본문을 호스트 LLM에
  넘기는 통로다. 지우면 모델이 서식 내용을 몰라 **지어낸다.**
  8/26 프리뷰에서 카드 정상 렌더를 실물 확인했다. 봉투 검사 테스트에 근거 주석 있음.
- ⛔ **`.fld{text-indent:0}` 제거 금지** — 지우면 서식 글자 겹침이 되살아난다.
- ⛔ **Kakao Tools MY에서 우리 서비스 '제거' 금지** — 재추가 불가.
- ⛔ **실기기 테스트 캡처·대화내역 외부 유출 금지** — 선공개 테스트. 팀 디스코드 밖으로 금지.
- ⛔ **서식 초안 채우기 문구를 세 번째로 고치지 말 것** — 9/2에 두 번 시도해 두 번 다 실패.
  모델이 매번 다른 이유를 댄다. 하게 된다면 방향은 하나뿐: `get_form_template`에 선택 인자
  `values`를 추가해 **서버가 채운 초안을 돌려주는 것**. 프리징 전에는 하지 않는다.
- ⛔ 주민등록번호 등 민감번호 6종을 채팅으로 받는 코드 금지(개발가이드 §5.3).
- ⛔ **9/16 전 "카카오톡에서 써보세요" 홍보 금지.** 계약 제9조: 공모전 언급은 "본선 진출작"까지만.
- ⛔ **description 길이를 줄이는 개편 금지.** 아린님 권고는 "간결하게"였으나
  8/22 프리뷰 실측에서 **호출 규칙을 맨 앞에 길게 쓴 쪽이 걸렸다.** 실측 없이 되돌리면 손해다.

## 배포·확인 방법 (상시 참조)

재배포는 **KakaoCloud MCP Hub**에 있다 — `playmcp.kakaocloud.io/mcp-detail/3539` 상단 우측.
(PlayMCP 콘솔 playmcp.kakao.com에는 재배포 버튼이 없다.)
⛔ 「중지」·「삭제」가 재배포 바로 옆이다. 누르지 말 것.

⚠️ **`Active`는 "최신"이라는 뜻이 아니다.** 새 빌드가 실패해도 옛 컨테이너가 살아 있으면
Active로 보인다. 9/1에 세 시간을 헤맨 이유다. 반드시 아래로 확인할 것:

```bash
curl -s https://legal-navigator-kakaotools.playmcp-endpoint.kakaocloud.io/healthz
```

기대값의 숫자(topics·forms·statutes…)가 다르면 배포가 안 올라간 것이다.
재배포는 즉시 반영이 아니다 — 수십 초~수 분 뒤에 바뀐다. 시간을 두고 다시 볼 것.

## 프리징 이후로 미룬 것

**생전 재산 이전(증여 vs 상속 vs 매매)** — 커버리지에 통째로 비어 있다.
있는 것은 전부 '돌아가신 뒤'(상속포기·재산분할·상속등기·유류분)뿐이다.

⚠️ **지금 넣지 않는다.** "뭐가 유리한가"는 절세 자문이고 세무사 영역이며,
우리 declaw 원칙("결론 단정 금지")과 정면으로 부딪힌다. 틀리면 수천만 원이 걸린다.

본선 후(10/12~) 만들 때의 방향 — 유불리는 말하지 않고 이것만 준다:
① 세 경로의 차이와 절차 ② 각 세목의 신고기한(증여세 3개월·상속세 6개월·양도세 2개월
— ★넣기 전 법제처 원문 재확인 필요) ③ 답이 갈리는 지점 체크리스트
④ 어디로 가야 하는지(국세청 126, 마을세무사 무료 상담).

## 팀원 검수 요청 (용우·예은) — 열린 채로 남음

- **서식 리뷰 1/114** — 8/6부터 열린 건. 은미님이 9/1에 121종을 자동검사+화면점검으로
  대신 훑었으나(→ 119종) 2순위 76종 본문 정독은 남아 있다.
- 법률 내용 검수: 새 주제 6종, 특히 `오인미만사업장`의 "노동위 구제 불가 /
  해고예고수당·임금·퇴직금은 가능" 구분.

## 관련 파일·링크

- 레포: `~/Projects/legal-navigator-mcp` (github.com/yuneunmi814-cmyk/legal-navigator-mcp, main)
- 지난 기록: `HANDOFF-archive.md`
- 팀 시트: `ka_mcp_법률절차길잡이`(용우님 소유)
- 마감 체크리스트: `KAKAO_TOOLS_FINAL.md` (⚠️ 소개문구·대화예시는 콘솔 저장본이 최신 — 이 인계장 기준으로 볼 것)
- 팀 GAP 분석: `GAP_ANALYSIS.md` (6개 전부 반영 완료 — 문서는 미반영 상태로 남아 있음)
- 검증 리포트: `audit/REPORT.md` · 재생성 `npx tsx scripts/verify-statutes.ts --statutes|--precedents`
- 검수 보고서: `audit/REVIEW_RESULT_0901_A.md` · `audit/UTTERANCE_TEST_0901.md`
- 콘솔 playmcp.kakao.com/console · 프리뷰 preview-chatgpt.kakao.com (은미님 계정 필요)
- 명령: `npm test` · `npm run typecheck` · `npm run build` · 로컬 `WIDGETS=on PORT=4188 node dist/server.js`
- 위젯 미리보기: `/widgets/{form|triage|calc|aid|procedure|checklist|deadline}` (aid는 `?lv=1`)
- 소개 이미지 원본: `assets/kakao-tools-examples/` · 사본 `~/Downloads/카카오툴즈_소개이미지/`
  ✅ **9/4 13:56 새 5장으로 콘솔 교체 완료**(진단·서식·전화·계산기·기한 D-day).
  다시 뽑을 땐 `npm run capture:intro` — 서버 응답을 그대로 그리므로 손으로 만들지 말 것.

## 콘솔 실물 확인 (2026-09-04 04:10)

playmcp.kakao.com/console — 로그인된 상태에서 **읽기만** 했다(아무것도 저장 안 함).

**항목이 둘이다. 헷갈리지 말 것.**

| 탭 | 서버 | 식별자 | 상태 | Tool call | 누적 사용자 |
|---|---|---|---|---|---|
| 등록된 MCP | `legal-navigator-full` (**예선**) | `legalNavi` | 심사 완료 · 전체 공개 | 55 | 325 |
| 임시 등록된 MCP | `legal-navigator-kakaotools` (**본선**) | `legalNaviTools` | **심사 전** | 0 | 1 |

- 본선 항목의 MCP 설명은 **이미 9/1 개정본**(279주제·119서식·102제도)이 들어가 있다.
  → 인계장에 남아 있던 "콘솔 설명 교체" 숙제는 **끝난 것**이다.
- 예선 항목 설명은 낡은 채(233주제·40종)지만 그건 예선판이라 그대로 두는 게 맞다.
- Kakao Tools 전용 항목 = **서비스 소개 문구(17/20)** + **소개 이미지 5장**(순서 변경 가능).
- 대표 이미지(저울 아이콘)는 이미 등록돼 있다.

⛔ **버튼 위치 주의 — 위험한 것이 항상 왼쪽에 하얗게 있다.**
- 상세 화면 하단: `[심사 요청] [수정] [삭제]`
- 수정 창 하단: `[등록 및 심사 요청] [저장하기] [취소]`
좌표로 누르지 말고 **글자를 보고** 누를 것.
