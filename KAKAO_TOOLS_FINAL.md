# Kakao Tools 본선 마감 체크리스트

> 기준 문서: `Kakao Tools 개발가이드 v1.0.0` + `Agentic Player 10 공모전 본선 가이드`
> 최종 확인: 2026-08-31

## 가장 중요한 구분

- **예선용**: `legal-navigator-full` — 삭제·중지·재배포하지 않고 기존 공개 상태로 유지한다.
- **본선용**: `legal-navigator-kakaotools` — 이 저장소의 최신 `main`을 기존 서버에 **재배포**한다.
- PlayMCP 본선용 등록은 **임시 등록**으로 두고, **등록 및 심사 요청을 누르지 않는다.**
- 소스를 고친 뒤에는 서버를 새로 만들지 말고 PlayMCP in KC에서 **재배포**한다.

## 일정

| 날짜 | 할 일 |
|---|---|
| 8월 31일 | 본선용 MCP 개발 완료 |
| 9월 1일~11일 | 카카오 QA 지적의 오류 위주 수정 |
| 9월 14일 | 코드 프리징 |
| 9월 16일~10월 12일 | 툴 챌린지 본선; 원칙적으로 서버 수정 불가 |

## PlayMCP 메타정보

- **서비스명**: 법률 절차 길잡이
- **서비스 설명(20자 이내)**: `생활법률 절차·서식 길잡이` (14자)
- **본선 Endpoint**: `https://legal-navigator-kakaotools.playmcp-endpoint.kakaocloud.io/mcp`
- **대화스타터**:
  1. `월급을 3개월째 못 받았어요. 어떻게 하나요?`
  2. `전세 보증금을 못 돌려받고 있어요.`
  3. `상속을 포기하고 싶어요. 절차가 어떻게 되나요?`
- **예시 이미지**: `assets/kakao-tools-examples/` 아래의 PNG 중 최대 5장. 로고가 아니라 실제 기능 화면을 등록한다.

## 재배포 순서

1. 로컬에서 `npm test`, `npm run typecheck`, `npm run build`를 모두 통과시킨다.
2. `legal-navigator-kakaotools`를 최신 `main`으로 재배포한다.
3. Redeploying이 Active로 바뀔 때까지 기다린다.
4. `/healthz` = `{"status":"ok"}`를 확인한다.
5. `tools/list`에서 16개 도구·독특한 영문 name·annotations 5종·description 1,024자 이하를 확인한다.
6. PlayMCP 개발자 콘솔에서 Endpoint **정보 불러오기**를 다시 실행한다.
7. 임시 등록 상태를 유지한 채 도구함에 추가한다.
8. Kakao Tools Preview에서 아래 사용자 흐름을 확인한다.

## 프리뷰 필수 시나리오

1. **진단**: `월급을 두 달째 못 받고 있어요`
   - `triage` 카드가 뜨고, 기한·지금 할 일·접수처가 한 화면에 보여야 한다.
2. **서식**: `임금체불 진정서 양식 보여줘`
   - 빈칸 채우기·접수처·HWPX 받기 버튼이 떠야 한다.
   - 주민등록번호·계좌번호는 채팅에 쓰지 말라는 안내가 보여야 한다.
3. **무료 도움**: `변호사 선임할 돈이 없어요`
   - 기본은 검증된 Card여야 하며, 전화 버튼을 누르면 `tel:` 연결이 돼야 한다.
   - ListView는 Preview에서 확인한 뒤에만 `LISTVIEW=on`으로 켠다.
4. **기한**: `상속포기 기한을 계산해줘`
   - 마감일·D-day·기산점·긴급 상담 버튼을 확인한다.
5. **일상어**: `야 나 잘렸어 어떡하지?`
   - 법률용어가 없어도 부당해고 관련 진단으로 연결돼야 한다.

매 테스트 후에는 **새 대화 시작**으로 맥락을 초기화한다.

## 개발가이드 준수사항

- Streamable HTTP·Remote·Stateless, MCP 2025-03-26 이상.
- 도구 16개: 20개 이하. 3~10개 권장은 초과하지만, 입력 스키마가 다른 계산·인용·서식 도구를 무리하게 합치지 않는다.
- 위젯 봉투는 `widget`, `copy_text`, `name`만 보내고 `status`는 보내지 않는다.
- `DEBUG_LOG`를 켜도 사용자 프롬프트·인자 값을 저장하지 않는다.
- 주민등록번호·운전면허번호·여권번호·외국인등록번호·카드번호·계좌번호를 채팅에서 묻거나 받아 적지 않는다.
- 서식의 민감번호는 사용자 기기의 문서 화면에서만 직접 입력하며 서버로 전송되지 않는다.
- 광고·홍보성 응답을 넣지 않는다.
- 평균 100ms 이내, p99 3,000ms 이하를 배포 전 실측한다.

## 현재 데이터 규모

- 57개 분야·268개 주제
- 표준서식 121종
- 판례 194건(고유 사건번호 184건)
- 법령 요지 262건
- 법률용어 125개
- 자동 테스트 225개
