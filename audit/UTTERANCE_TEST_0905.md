# 발화 테스트 결과 — 계약·용역·동업 미이행 계열 라우팅

- **일시**: 2026-09-04 (9/1 문서의 후속)
- **검증 대상**
  - 소스: `main` `b7c7658` (로컬에서 `app.listen(0)`으로 직접 구동 — 네트워크·호출한도 없음)
  - 본선 배포: `https://legal-navigator-kakaotools.playmcp-endpoint.kakaocloud.io/mcp`
  - 실사용 경로: `https://preview-chatgpt.kakao.com/` (ChatGPT for Kakao 프리뷰, MCP 자동 연결)
- **시험한 발화**: 18개 (O 2 · X 16) + 프리뷰 실대화 4건
- **호출 수**: 본선 서버 18회 / 한도 60 · 호출 간 1.2초 간격
- **수정 범위**: `SEARCH_SYNONYMS` 데이터만. 매칭 로직(`rankTopics`)은 한 줄도 건드리지 않았습니다.
- **결과**: 18개 중 2개 통과 → **18개 전부 통과**. 기존 테스트 포함 **242개 전부 초록불**.

> **로컬과 본선 서버 결과가 실패 항목까지 완전히 일치**했습니다. 로컬에서 고치면 배포에 그대로 반영됩니다.
> 예선 서버 `legal-navigator-full`은 `/healthz`에 `scale` 필드조차 없어 한참 구버전입니다(같은 발화가 `재판이혼`으로 갔습니다). 이 문서의 수치는 **본선 서버 기준**입니다.

---

# 🔴 실사용 경로에서 확인된 것 — 도구가 답변을 더 나쁘게 만들었습니다

같은 사건을 말투만 바꿔 두 번 물었습니다. **디자인 외주를 납품했는데 거래처가 잔금을 안 주는 상황**입니다.

| | 짧게 물었을 때 | 길게 상담하듯 물었을 때 |
|---|---|---|
| 도구 호출 | **없음** | `triage` 호출됨 |
| 뜬 카드 | 없음 | **프리랜서·위탁계약인데 근로자로 인정받을 수 있을까** [노동] |
| ChatGPT 결론 | 내용증명 → **지급명령** → 소액사건 | **근로자성 인정 여부**로 유도 |
| 안내된 접수처 | 법원 | **노동위원회** |

**도구를 부르지 않았을 때가 정확했습니다.** 잔금 청구 사건인데 근로자성 카드가 떠서, 답변이 노동 쪽으로 끌려갔습니다.

## 왜 한 번 틀리면 회복이 안 되는가

`triage` 응답의 진행 지침에 다음이 들어갑니다.

```
- **주제는 이미 "프리랜서근로자성"으로 정해졌다. 이 대화에서 triage나 search_topics를
  다시 호출하지 말 것.**
```

맥락 유실을 막는 좋은 설계입니다. 다만 **첫 진단이 틀리면 그 대화 전체가 틀린 주제로 잠깁니다.** 실제로 이어진 응답이 이랬습니다.

```
하나만 확인할게요 — 노동위원회 구제신청은 해고일이 언제쯤인가요?
 ① 최근 1개월 안  ② 6개월 안  ③ 그보다 오래됐어요  ④ 직접 입력
```

해고된 적이 없는 사람에게 해고일을 묻습니다. 함께 주입되는 근거법·접수처도 근로기준법 제2조·제28조와 **노동위원회 minwon.nlrc.go.kr**입니다.

**9/1 문서에서 🔴로 표시한 '각하 경로'와 같은 종류입니다.** 잔금을 못 받은 프리랜서가 노동위원회에 가면 "당신은 근로자가 아니다"에서 막힙니다.

## 정답은 같은 응답 안에 이미 있었습니다

```
_상황이 다르다면: 물건값·일한 대금을 못 받았을 때 (미수금)_
```

`물품용역대금미수금`이 2순위로 나옵니다. **1·2등 순서만 뒤집혔는데, 하필 1등이 잠금되어 대화 전체가 그쪽으로 굳었습니다.**

---

# 발화 18개 — 2/18 통과

기대 주제는 넉넉하게 잡았습니다(아래 후보 중 아무거나 1순위면 ✅).

## 상대 미이행(계약 위반)

| 발화 | 기대 | 실제 결과 | 판정 |
|---|---|---|---|
| 계약 상대방이 일을 안 해서 손해를 봤다 | 채무불이행 계열 | 채무불이행 | ✅ |
| 동업자가 자기 몫 일을 안 하고 잠수타서 내가 대신 처리했다 | 채무불이행 계열 | **연대보증채무대응** | ❌ |
| 공동수급자가 마감을 안 지켜서 내가 대신 외주비를 썼다 | 채무불이행 계열 | **어느 주제에도 못 닿음** | ❌ |
| 외주업체가 납기를 못 맞춰서 손해가 났어요 | 채무불이행 계열 | **어느 주제에도 못 닿음** | ❌ |
| 같이 일하기로 한 사람이 연락이 두절인데 어떻게 하나요 | 채무불이행 계열 | **어느 주제에도 못 닿음** | ❌ |
| 용역 계약했는데 상대가 약속한 작업을 안 끝냈어요 | 채무불이행 계열 | **어느 주제에도 못 닿음** | ❌ |

## 일한 대금 미수

| 발화 | 기대 | 실제 결과 | 판정 |
|---|---|---|---|
| 일해준 대금을 못 받고 있어요 | 물품용역대금미수금 | 물품용역대금미수금 | ✅ |
| 프리랜서로 작업 납품했는데 대금을 안 줘요 | 물품용역대금미수금 | **프리랜서근로자성** | ❌ |
| 용역비를 3개월째 안 주는 거래처 | 물품용역대금미수금 | **어느 주제에도 못 닿음** | ❌ |
| 디자인 외주를 끝냈는데 잔금을 안 줍니다 | 물품용역대금미수금 | **어느 주제에도 못 닿음** | ❌ |

## 대신 지출한 비용 회수(구상)

| 발화 | 기대 | 실제 결과 | 판정 |
|---|---|---|---|
| 상대방이 할 일을 내가 대신 하고 쓴 비용을 돌려받고 싶어요 | 채무불이행·부당이득 | **임의동행보호조치** | ❌ |
| 동업자 대신 낸 외주비 16만원을 청구하려면 어떻게 하나요 | 채무불이행·부당이득 | **연대보증채무대응** | ❌ |
| 제가 대신 부담한 비용을 상대에게 돌려받을 수 있나요 | 채무불이행·부당이득 | **어느 주제에도 못 닿음** | ❌ |

## 받아내는 절차

| 발화 | 기대 | 실제 결과 | 판정 |
|---|---|---|---|
| 16만원 받으려고 소송하는 게 나을까요 | 지급명령·소액사건 | **고소고발진정신고차이** | ❌ |
| 적은 금액인데 법적으로 받아낼 방법이 있나요 | 지급명령·소액사건 | **어느 주제에도 못 닿음** | ❌ |
| 내용증명을 보냈는데 답이 없으면 다음은 뭔가요 | 지급명령·소액사건 | **어느 주제에도 못 닿음** | ❌ |
| 돈 받아내는 절차 중에 제일 빠른 게 뭐예요 | 지급명령·소액사건 | **어느 주제에도 못 닿음** | ❌ |

## 실제 사건 전문(긴 자연어)

프리랜서 두 명이 고객사와 5:5로 공동계약했는데, 한쪽이 자기 몫을 이행하지 않고 연락이 끊겨 다른 쪽이 대신 마무리하고 외주비 16만원을 지출한 사안입니다.

| 기대 | 실제 결과 | 판정 |
|---|---|---|
| 채무불이행·미수금·지급명령 계열 | **연대보증채무대응** (예선 서버에서는 **재판이혼**) | ❌ |

---

# 대조군 — 기존 주제는 그대로입니다

넓히는 변경을 할 때 이쪽이 깨지지 않는지 함께 봐야 합니다.

| 발화 | 실제 결과 | 판정 |
|---|---|---|
| 친구한테 빌려준 돈 못받았어 | 대여금미반환 | ✅ |
| 친구한테 돈 빌려줬는데 안 갚아요 | 대여금미반환 | ✅ |
| 계약 상대방이 일을 안 해서 손해를 봤다 | 채무불이행 | ✅ |

기존 테스트 **241개는 전부 통과**합니다(새로 추가한 이 항목만 실패).

---

# 원인 (코드를 읽고 대조한 결과)

`rankTopics` (`src/server.ts:377`)

## 1) 0건 10건 — 게이트가 아니라 동의어가 없습니다

`물품용역대금미수금`("물건값·일한 대금을 못 받았을 때")은 멀쩡히 있는데, **거기로 가는 말이 없습니다.**

| 빠진 표현 | 닿았어야 할 주제 |
|---|---|
| 용역비 / 용역 계약 | 물품용역대금미수금 · 채무불이행 |
| 외주 / 외주업체 / 외주비 | 물품용역대금미수금 · 채무불이행 |
| 납품 / 납기 | 물품용역대금미수금 |
| 거래처 / 잔금 | 물품용역대금미수금 |
| 공동수급자 / 동업자 | 채무불이행 |
| "받아낼 방법" / "내용증명 다음" | 지급명령 · 소액사건소송 |

**0건 자체는 버그가 아니라 설계대로 동작한 것입니다.** 근거가 없으면 답하지 않는 쪽이 맞습니다(`CLAUDE.md` 절대규칙 1). 게이트를 푸는 게 아니라 데이터를 채우는 문제로 보입니다.

## 2) 어절을 조사째로 substring 매칭합니다

`words`가 공백으로만 쪼갠 어절이고, 이것을 `적용대상` 문장에 그대로 `includes`로 겁니다.

실제 사건 전문에 대한 점수입니다.

```
재판이혼    ← "상대방이"(+2) "자기"(+2)      = 4점 / 근거 2개
채무불이행  ← "상대방이"(+2) "이행하지"(+2)  = 4점 / 근거 2개   ← 동점
```

동점이라 데이터 파일 등록 순서로 1등이 갈립니다. **"상대방이"·"자기"·"않고"·"대신"** 같은 조사 붙은 기능어가 근거로 잡히고, `weak >= 2` 게이트를 **그 둘만으로** 통과합니다.

주석에 남아 있는 전적(계속→헬스장, 연락→불법추심, 업무누락→보이스피싱)과 같은 계열이고, 한 겹 아래입니다.

## 3) `hay`에 근거법 전문이 들어가 긴 주제가 유리합니다

```ts
const hay = `${p.적용대상} ${p.근거법.join(" ")}`;
```

`연대보증채무대응`은 근거법 텍스트가 1,000자에 가깝습니다. 그 안의 "3개월 이상 **이행하지** 않으면"(보증인보호법 제5조①), "주채무자 **대신**"이 걸려 6점을 얻었습니다. **주제 설명이 길수록 아무 말이나 얻어걸릴 확률이 올라갑니다.** 18개 중 3개가 이 주제로 갔습니다.

---

# 고친 내용 — 동의어 데이터만

원인 3개 중 **①(동의어 구멍)만** 고쳤습니다. `src/data/legal_updates.ts`의 `SEARCH_SYNONYMS` 한 파일이고, 매칭 로직은 건드리지 않았습니다.

| 덩이 | 넣은 말 | 왜 |
|---|---|---|
| 어미 변형 | `잔금을 안 줍` · `대금을 안 줍` · `잔금을 못 받` | `잔금을 안 줘`는 있는데 `줍니다`가 안 걸렸습니다(55행 주석과 같은 문제) |
| 받은 쪽 | `용역비` · `외주비` · `외주를` · `거래처` · `납품했는데` · `페이를 안` · `클라이언트가` | 내가 일하고 대금을 못 받은 경우 |
| 발주한 쪽 | `외주업체` · `납기` · `약속한 작업` · `일을 안 해` | **입장이 반대라 주제도 다릅니다** — 채무불이행·계약해제해지 |
| 동업·구상 | `공동수급` · `동업자` · `대신 처리했` · `돌려받고 싶` | 전용 주제가 없어 가장 가까운 `채무불이행`으로 |
| 절차 | `받아낼 방법` · `내용증명 보냈는데` | 지급명령·소액사건소송·민사조정 |

## 삭제 한 건 — `"프리랜서"` 단독 트리거

`SEARCH_SYNONYMS`에서 `"프리랜서"` 한 단어만으로 `프리랜서근로자성`에 걸던 것을 뺐습니다(89행). **프리랜서가 대금·계약을 물어도 근로자성으로 끌려가던 직접 원인**입니다. 근로자성이 실제 쟁점인 표현(`근로자성`·`위탁계약`·`3.3%`·`출퇴근 다 찍`·`지시대로만 일` 등)은 그대로 두었습니다.

`프리랜서 + 퇴직금/임금` 조합은 `rankTopics`의 근로자성 강제주입 규칙이 따로 처리하므로 그 경로는 유지됩니다. 9/1 문서의 근로자성 케이스 4개를 다시 돌려 확인했습니다.

| 발화 | 결과 |
|---|---|
| 3.3% 떼고 받았는데 저 근로자 아닌가요 | 프리랜서근로자성 ✅ |
| 위탁계약서 썼지만 출퇴근 다 찍었어요 | 프리랜서근로자성 ✅ |
| 프리랜서인데 4대보험도 안 되고 퇴직금도 못 받나요 | 프리랜서근로자성 ✅ |
| 프리랜서 계약인데 해고당했어요 | 프리랜서근로자성 ✅ |

## 어미가 달라도 걸리는지 따로 확인했습니다

동의어를 어미별로 다 넣는 것은 끝이 없습니다. 실제로 걸리는 이유는 **명사 단서**(`외주를`·`용역비`·`거래처`)가 어미와 무관하게 매칭되기 때문입니다. 변형 10개로 확인했습니다.

| 발화 | 결과 |
|---|---|
| 디자인 외주를 끝냈는데 잔금을 안 주네요 | 물품용역대금미수금 ✅ |
| 디자인 외주를 끝냈는데 잔금을 안 주셔서요 | 물품용역대금미수금 ✅ |
| 외주 일 끝냈는데 잔금 안 줌 | 물품용역대금미수금 ✅ |
| 용역비를 지급하지 않습니다 / 미지급했어요 | 물품용역대금미수금 ✅ |
| 작업 다 했는데 돈을 안 보내줘요 | 물품용역대금미수금 ✅ |
| 납품 다 했는데 결제가 안 됐어요 | 물품용역대금미수금 ✅ |
| 거래처가 대금을 계속 미루고 있어요 | 물품용역대금미수금 ✅ |
| 프리랜서인데 클라이언트가 페이를 안 줘요 | 물품용역대금미수금 ✅ |

10개 중 10개 통과입니다.

## 고치는 중에 나온 별개 버그 — `"고소"`가 어절 경계를 넘습니다

`"16만원 받으려고 소송하는 게 나을까요"`가 **고소고발진정신고차이**로 갔습니다. 동의어는 공백을 지운 뒤 부분문자열로 보므로, `받으려` + `고 소` + `송`에서 **`고소`가 만들어집니다.** 2글자 동의어라 이런 조합이 더 있습니다(`빌려주고 소송`, `참고 소송`).

근본 수정은 매칭 로직 쪽이라 **이 PR에서는 고치지 않았습니다.** 민사 절차 동의어를 `"고소"` 항목보다 앞에 두어 이 케이스만 막았고, 주석으로 남겼습니다.

---

# 라우팅과 별개로 드러난 커버리지 구멍

라우팅을 고쳐서 `채무불이행`·`물품용역대금미수금`으로 제대로 보내더라도, **그 다음에 줄 것이 비어 있습니다.** 위 사건(공동수급자 미이행 → 대신 이행 → 실비 청구)을 끝까지 따라가며 확인했습니다.

## 1) 동업·조합 관계를 다루는 주제가 없습니다

| 확인한 것 | 결과 |
|---|---|
| 주제 키에 `동업\|조합\|공동\|구상\|정산` | ❌ 없음 |
| 조합 규정(민법 제703~724조)을 근거법에 쓴 주제 | `교통사고손해배상청구` 1건뿐(제724조 직접청구권) |
| '동업'이라는 말이 나오는 주제 | `연대보증채무대응` 1건 — 그것도 **보증인보호법 제2조의 적용제외 사유**("동업자 보증")로만 |

5:5 공동계약에서 한쪽이 이행하지 않았을 때의 정산·구상은 `채무불이행`으로 **근사할 수 있을 뿐** 정조준하는 주제가 없습니다. 조합 관계라면 민법 제712조(손실부담)·제713조(무자력 조합원의 채무)가 걸리는데 어느 주제에서도 다루지 않습니다.

덧붙여, 이용자가 "동업자가…"로 말을 시작하면 그 말이 실제로 존재하는 유일한 곳이 `연대보증채무대응`의 근거법이라는 점도 오라우팅과 무관하지 않아 보입니다.

## 2) 대금 청구 주제에 연결된 서식이 0개입니다

```
물품용역대금미수금 → 연결 서식 ❌ 없음
채무불이행         → 연결 서식 ❌ 없음
```

두 주제 모두 `get_procedure` 응답의 진행 지침에 이렇게 나갑니다.

```
- 이 주제에는 제공할 표준 서식이 없다. get_form_template을 호출하지 말고,
  서식을 지어내지도 말 것.
```

지어내지 않는 것은 옳습니다. 다만 **서식 실물이 이 서비스의 차별점인데, 돈 받아내는 경로에서만 비어 있습니다.**

## 3) 서식은 있는데 닿지 않습니다 — 매핑이 1:1입니다

`FORM_TOPIC`이 `서식 → 주제` 1:1 구조라(`src/server.ts:141`), 서식 하나가 여러 주제에 걸리지 못합니다.

| 서식 | 현재 걸린 주제 | 대금 주제에서 |
|---|---|---|
| `지급명령신청서` | `지급명령` | ❌ 안 나옴 |
| `소액소송_소장` | `소액사건소송` | ❌ 안 나옴 |
| `손해배상청구_소장` | `손해배상청구` | ❌ 안 나옴 |

이용자가 "잔금을 못 받았다"에서 출발하면 `물품용역대금미수금`에 도착하는데, 정작 필요한 `지급명령신청서`는 **주제를 한 번 더 갈아타야** 나옵니다.

## 4) 없는 서식 — 용역대금·미수금 내용증명

현재 내용증명 서식 10종입니다.

```
임금지급 · 보증금반환 · 대여금반환 · 교통사고_손해배상 · 수선요구
장충금반환청구 · 계약해제 · 부동산매매_해제 · 상가_갱신요구 · 개물림_손해배상
```

**용역대금·미수금·외주비 청구용이 없습니다.** `채무불이행` 절차의 2단계가 "내용증명 이행 최고(기한 없는 채무는 최고로 지체 발생)"이고 민법 제397조상 금전채무는 최고 시점이 지연이자 기산점이 되는데, 정작 그 단계에서 내밀 서식이 없습니다. 이용자가 실무에서 가장 먼저 필요로 하는 문서입니다.

## 우선순위에 대한 의견

라우팅 수정은 **잘못된 안내를 없애는 일**이고, 커버리지는 **줄 것을 만드는 일**입니다. 잘못된 안내를 먼저 없애는 순서가 맞다고 봅니다. 다만 라우팅만 고치면 "맞는 주제에 도착했는데 서식이 없다"로 끝나므로, 최소한 3)의 매핑 문제는 함께 보는 것이 좋겠습니다. 서식을 새로 만들지 않고 연결만 바꾸면 되는 부분입니다.

---

# 9/1 문서와의 관계

9/1에 이미 약점으로 기록된 두 주제가 이번 오라우팅 6건 중 4건입니다.

| 9/1 기록 | 이번에 관측된 것 |
|---|---|
| 프리랜서·근로자성 4개 중 2개 성공 · "프리랜서인데 퇴직금" → 퇴직금으로 샘 (🟠 순서 뒤바뀜) | **반대 방향으로도 샙니다** — 대금청구가 근로자성으로. 실사용 경로에서 🔴 각하 경로 |
| 연대보증 4개 중 2개 성공 | 무관한 계약 분쟁 3건이 **연대보증으로 흡수** |

---

# 재현 방법

```bash
# 로컬 (네트워크·호출한도 없음)
npx vitest run test/server.test.ts -t "계약·용역·동업"

# 본선 서버 (호출 간 1.2초 권장)
curl -s -X POST https://legal-navigator-kakaotools.playmcp-endpoint.kakaocloud.io/mcp \
  -H "Content-Type: application/json" -H "Accept: application/json, text/event-stream" \
  -d '{"jsonrpc":"2.0","id":1,"method":"tools/call","params":{"name":"search_topics","arguments":{"query":"디자인 외주를 끝냈는데 잔금을 안 줍니다"}}}'
```

추가한 테스트는 `test/server.test.ts:534`입니다. 18개 발화를 돌려 **실패한 것만 모아서 한 번에 출력**하므로, 고친 뒤 통과율이 바로 보입니다.

---

# 2차 — 카페 실사용 제목 202개 (9/7)

지어낸 발화가 아니라 **실제로 올라온 글 제목**으로 다시 쟀습니다. 채무·전세 카페 두 곳(`ccrs5500`·`cafe20135031`)에서 좋아요 상위 글의 제목 202개입니다. 원문은 커밋하지 않습니다(스크래퍼 노트의 방침).

```
주제 찾음  159 → 175   (79% → 87%)
0건         43 →  27
```

**0건 43개 중 절반 이상은 0건이 정답이었습니다.** 상담 발화가 아니라 후기·정보공유·잡담이기 때문입니다.

| 종류 | 예 | 판정 |
|---|---|---|
| 정보공유·후기 | `우편물 방어 Tip!!` · `드디어 오늘 워크아웃 체결했습니다 대박` | 0건이 맞음 |
| 감정·잡담 | `채무자의자세!` · `좀 많이 복잡한데 도와주실분 찾아요~` | 0건이 맞음 |
| **진짜 놓침** | `임차권등기 이후 지연이자` · `명도일이 걱정이에요` | ❌ 주제가 있는데 못 닿음 |

판정 기준은 **"카톡 입력창에 사람이 실제로 칠 법한 문장인가"** 로 잡았습니다. 게시판 제목 특유의 말투(`Tip!!`, `공유드립니다`, `완전 정복`)는 상담창에 들어오지 않습니다.

## 원인 — 실무에서 쓰는 말과 법률 용어가 다릅니다

주제는 **전부 이미 있었습니다.** 거기로 가는 말이 없었을 뿐입니다.

| 사람이 쓰는 말 | 데이터의 말 | 주제 |
|---|---|---|
| **명도** | 인도 | `건물인도청구` |
| **원상복구** | 원상회복 | `원상회복범위` |
| **압류방어** | 압류방지 | `압류방지통장` |
| **보증보험** | 반환보증·이행청구 | `전세보증보험이행청구` |
| 임차권등기 · 신용회복지원 · 낙찰 | (없음) | 각 주제 |

1차(용역비·외주비·거래처)와 **완전히 같은 종류**입니다.

## 고친 것

`SEARCH_SYNONYMS`에 5덩이 추가·2덩이 확장. 놓쳤던 12개 중 **10개가 해결**됐습니다.

| 발화 | 이제 |
|---|---|
| 임차권등기 이후 지연이자 | 임차권등기명령 ✅ |
| 도와주세요 터무니없는 원상복구비용 | 원상회복범위 ✅ |
| 보증보험기간 만료됐다네요 / 중도해지합의서 및 보증이행절차 / 최종 이행 거절 받았습니다 | 전세보증보험이행청구 ✅ |
| 명도통보 내용증명 / 명도일이 걱정이에요 | 건물인도청구 ✅ |
| 압류방어용 통장개설 | 압류방지통장 ✅ |
| 대학생 청년 신용회복지원 | 신용회복위원회채무조정 ✅ |
| 경매 셀프 낙찰 자금 마련 | 임차인경매대응 ✅ |

## 곁다리로 나온 버그 — `matchFormsByName`이 낱말 하나에 막힙니다

```
matchFormsByName("내용증명")       → 13건
matchFormsByName("내용증명 양식")  →  0건
```

**모든 낱말이 서식 이름에 있어야 통과하는 구조**(`toks.every`)라, `양식` 하나가 붙으면 통째로 0건이 됩니다. 이 fallback은 "문서 이름으로 찾는 사람"을 위해 만든 것인데(주석에 그렇게 적혀 있습니다), 사람들은 문서 이름 뒤에 `양식`·`서식`·`샘플`을 붙여서 씁니다.

군더더기 낱말 9개를 토큰에서 빼는 것으로 고쳤습니다(`src/server.ts`). 군더더기만 남으면 전량 매칭이 되므로 그때는 찾지 않습니다. 이 PR에서 **매칭 로직을 건드린 유일한 곳**입니다.

## 네거티브에서 제외한 둘

`서울 서부 명도 완료 타임라인`과 `2014년 2분기 신용회복지원 실적`은 이제 주제로 매칭됩니다. 다만 **상담창에 칠 문장이 아니라 실해가 없고**, 0건으로 만들려면 `명도`·`신용회복지원`을 좁혀야 하는데 그러면 `명도일이 걱정이에요`가 죽습니다. 그대로 두었습니다.

---

# 3차 — 질문글 195개 (9/7). 뽑는 기준을 바꿨습니다

2차까지는 **좋아요 상위**로 뽑았습니다. 그런데 좋아요는 정보공유·후기·팁에 몰리고 **질문글에는 붙지 않습니다.** 2차 202개의 좋아요 중앙값이 11, 최대 839였고, 0건 43개 중 절반 이상이 상담이 아니라 후기였던 이유가 이것이었습니다.

스크래퍼의 `sortBy=LIKE`를 인자로 빼고 기본을 최신순으로 바꿔 다시 받았습니다(`scripts/collect_ccrs5500_top.py`).

```
좋아요순 202개 → 좋아요 중앙값 11 · 최대 839 · 0인 글 3개
최신순  195개 → 좋아요 중앙값  0 · 최대  14 · 0인 글 131개
```

제목이 전부 질문입니다 — `도와주세요` `조언 부탁드려요` `괜찮을까요?` `여쭤봅니다` `궁금한 점`.

## 결과 — 0건보다 오라우팅이 더 큰 문제였습니다

응답 45개를 직접 읽고 판정했습니다.

| | 개수 | 성격 |
|---|---|---|
| 0건 | 52 (27%) | 못 찾음. **"찾지 못했습니다"로 정직하게 답함** |
| └ 답했어야 함 | 26 | 보정명령·심사반송·예납금·실효 등 |
| └ 애매·영역 밖 | 11 | |
| └ 0건이 정답 | 15 | 후기·잡담·스팸 |
| **오라우팅** | ~18 | **틀린 곳으로 보냄** |

0건은 무응답이고 오라우팅은 오답입니다. **후자가 더 위험합니다.**

## 고친 것 ① 소상공인 제도를 개인에게 안내하던 것

```
새출발기금          적용대상: 소상공인·자영업자
신용회복위원회채무조정   적용대상: 개인
```

그런데 `신속채무조정`·`프리워크아웃`·`개인워크아웃`이 두 주제에 함께 묶여 있었고, 동점일 때 앞자리인 새출발기금이 이겼습니다.

```
새출발기금  ← [개인]오늘 개인워크아웃(무직 주부) 체결했어요
새출발기금  ← [프리워크아웃_1편] 프리워크아웃이란?
```

**무직 주부가 자영업자 대상 제도를 안내받습니다.** 두 덩이로 분리했습니다. 195개 중 12개가 바로잡혔습니다.

## 고친 것 ② `"소액"` 두 글자가 대출 문의를 압류로 보내던 것

```
재산명시신청  ← 소액생계비대출 신청
부동산가압류  ← 신복위 소액대출
부동산가압류  ← 잇다앱 불법사금융예방대출(소액생계비대출) 당일방문 신청되나요
```

**돈을 빌리려는 사람에게 재산명시·가압류 안내가 나갔습니다.** 방향이 정반대입니다. `"소액"`을 `"소액사건"`·`"소액소송"`으로 좁혔습니다. 이제 0건이 되는데, 틀린 답보다는 낫습니다.

## 하지 않기로 한 것 — `hay`에 `단계`·`비고` 추가

`보정명령`이 `민사소송소장`의 **비고**에만 있고 점수 계산에는 `제목`·`분야`·`적용대상`+`근거법`만 쓰이는 것을 확인하고, `단계`·`비고`를 점수에 넣어 실험했습니다.

| | |
|---|---|
| 0건 | 52 → 46 (6개 회수) |
| 기존 테스트 | **244개 전부 통과** |
| 실제 변화 | 13개 중 **8개가 나빠짐** |

```
(0건)   → 압수수색적법성       성질 못 참고 화를 표시하게되면
(0건)   → 공무원불친절부당대우   신속 실효 중에 햇살론 카드 쓸 수 있나요?
(0건)   → 국가유공자_등록보훈    재도전교육 감면 관련 질문(새출발 추가감면)
(0건)   → 대출빙자형          신용회복중입니다. 완제 후 대출 문의
새출발기금 → 국가장학금_학자금대출  새출발 부실우려 진행중인데 추가 대출
개인회생  → 불법추심대응        [회생]금지명령 안나왔는데
```

빚 상담이 압수수색·국가유공자·보이스피싱·학자금대출로 갔습니다. `비고`는 주의사항을 자유롭게 적는 필드라 온갖 말이 들어 있습니다. 게다가 **정작 목표였던 `보정명령`은 여전히 0건**입니다 — 낱말 하나로는 2점·근거 1개라 게이트(4점·근거 2개)를 넘지 못합니다. 되돌렸습니다.

## 이번에 배운 것 두 가지

**① 0건 개수는 지표로 쓸 수 없습니다.** 0건을 줄이는 가장 쉬운 방법이 "아무 데나 보내기"라, 지표만 보면 개선으로 보입니다. 위 실험이 정확히 그랬습니다.

**② 기존 테스트 244개가 다 통과해도 안전하지 않습니다.** 8개가 나빠졌는데 하나도 잡지 못했습니다. 실사용 발화 세트가 없었으면 모르고 넘어갔을 변경입니다.

## 남은 빈틈 — 동의어로 해결되지 않는 영역

채무조정에 **이미 들어간 사람들**의 질문이 통째로 비어 있습니다. 지금까지 검증한 것은 전부 "들어가기 전" 발화였습니다. 카테고리가 아니라 **깊이**가 빈 것입니다.

| 사람들이 매일 묻는 말 | 데이터에 |
|---|---|
| 수정조정 · 재조정 · 완제 · 예납금 · 납부유예 | ❌ 없음 |
| 연체코드 · 신용도판단정보 · 심사반송 · 장기연체 | ❌ 없음 |
| 실효 | `형실효_범죄경력`(전과 말소)에만 — **뜻이 정반대** |
| 보정명령 | `민사소송소장`의 비고에만 (점수에 안 쓰이는 필드) |

`보정명령`은 기한을 넘기면 각하되는 사안인데 지금은 아무것도 안내하지 못합니다. **우리 서비스가 가장 잘해야 하는 종류(기한·절차)인데 비어 있습니다.**

동의어만 넣으면 "도착했는데 답이 없는" 상태가 되므로 이 PR에서는 손대지 않았습니다. 근거 출처(신용회복위원회 협약·회생법원 실무준칙)를 어디로 할지부터 정해야 합니다.

---

# ⚠️ 이 문서의 2·3차 수치는 게시판 제목 기준입니다 (재측정 결과 뒤집힘)

아래 2차(202개)·3차(195개) 측정은 **네이버 카페 게시판 글 제목을 그대로** `search_topics`에 넣은 것입니다. 실사용 경로는 `사용자가 카톡에 쓴 말 → 호스트 LLM 요약 → triage`이고, **게시판 제목은 그 어느 쪽도 아닙니다.**

같은 사안 25개를 세 형태로 다시 쟀습니다.

| 입력 형태 | 정답 | 0건 | 오답 |
|---|---|---|---|
| 게시판 글 제목 | 9 | **16** | 0 |
| 사람이 카톡에 칠 말 | **14** | 9 | 2 |
| LLM 요약 (실사용 경로) | 12 | 5 | **8** |

**제목 기준 측정은 두 방향으로 틀렸습니다.**

- **0건을 부풀립니다.** 제목은 `[개인]수정조정신청하려면...` 처럼 축약돼 있어 덜 매칭됩니다. 제목 16개 vs 실제 발화 9개.
- **오답을 숨깁니다.** 제목에는 범용어가 없어 노이즈 매칭이 안 일어납니다. 제목 0개 vs 실사용 8개.

따라서 아래 2·3차의 `79%`·`87%`·`0건 27%` 같은 수치는 **참고용으로만** 봐주시고, 실사용 정확도로 읽지 말아주세요. 어휘 빈틈(명도·용역비·수정조정·보정명령 등)과 고친 오라우팅 2건은 낱말 단위로 개별 확인한 것이라 그대로 유효합니다.

## ~~요약 단계가 만든 오답 8개~~ → 취소합니다 (2026-09-08 실측)

> **이 절은 틀렸습니다.** 여기 있던 표는 제가 "호스트 LLM이 이렇게 요약할 것"이라고 **지어낸 문장 25개**를 넣어 얻은 결과였습니다. 프리뷰에서 실제 요약문을 확인해 보니 형태가 달랐습니다.

실제 `triage`에 넘어간 `situation`(위젯 부제목에 그대로 노출됩니다):

```
"임대차계약 종료 후 명도일이 다음 주인데 임대인이 보…"
"프리랜서 디자인 외주를 6주간 수행하고 전부 납품했으…"
"프리랜서 공동계약에서 공동업무 상대방의 미이행으로 내…"
```

**사실을 압축한 서술문**입니다. 제가 만든 `무직 주부의 개인워크아웃 신청 자격 문의` 같은 명사형이 아니었습니다. `문의`·`대응`·`방법`으로 끝나는 형태를 제가 지어냈고, 그 범용어가 노이즈를 만든 것입니다. **실사용에서 그런 요약이 나온다는 증거가 없습니다.**

같은 발화를 프리뷰에서 실제로 돌린 결과는 오히려 정확했습니다.

| 발화 | 내가 지어낸 요약으로 잰 결과 | 프리뷰 실측 |
|---|---|---|
| 주부인데 개인워크아웃 신청할 수 있나요 | 체류기간연장변경 ❌ | `get_procedure` → 신용회복위원회채무조정 ✅ |
| 소득이 적은데 부양가족 넣어야 하나요 | 정신질환_비자의입원_심사 ❌ | `get_procedure` → 개인회생 ✅ |
| 추심원이 집에 온다는데 어떻게 해야해요 | (0건) | `get_procedure` → 불법추심대응 ✅ |

**렌터카 사례는 여전히 사실입니다** — 실사용 대화에서 실제로 일어났고 재현됩니다. 다만 그것을 일반화해 "요약이 오답을 만든다"고 8건으로 부풀린 것이 잘못이었습니다.

## 대신 확인된 것 — 실사용은 `get_procedure` 직접 호출이 더 흔합니다

프리뷰 8건(ChatGPT 메모리를 끈 상태):

| 발화 | 호출 | 결과 |
|---|---|---|
| 디자인 외주 끝냈는데 잔금을 안 줍니다 | `get_procedure` | 물품용역대금미수금 ✅ **(이 PR 수정분)** |
| 명도일이 다가오는데 어떻게 해야하나요 | `get_procedure` | 건물인도(명도)청구 ✅ **(이 PR 수정분)** |
| 윗집 층간소음 때문에 너무 힘들어요 | `get_procedure` | 층간소음분쟁 ✅ |
| 채무조정 수정조정 신청하고 싶은데 | `get_procedure` | 신용회복위원회채무조정 ✅(근사) |
| 친구한테 빌려준 돈 못 받았어 | `triage` | 대여금미반환 ✅ |
| 법원에서 뭐가 날아왔는데 | 없음 | 뭘 받았는지 안 밝힘 |
| 법원에서 보정명령 왔는데 뭘 해야해요 | 없음 | 주제 없음 |
| 카드값 밀렸는데 연체 기록 남나요 | 없음 | 주제 없음 |

```
호출 5 / 무호출 3 · 호출되면 5/5 정확
```

**`get_procedure`의 `topic`은 279개 enum이라 호스트 LLM이 직접 고릅니다. 이 경로는 `rankTopics`를 거치지 않습니다.** `search_topics`·`triage`만 재면 실사용의 일부만 보는 셈입니다.

**이 PR의 수정 두 건(`잔금`→미수금, `명도`→건물인도청구)이 프리뷰에서 확인됐습니다.**

## ⚠️ 프리뷰 측정에는 함정이 있습니다 — ChatGPT 메모리

프리뷰는 실제 ChatGPT 계정으로 로그인되고, **"새 대화 시작"은 화면만 지웁니다.** 계정 메모리는 남습니다.

| 발화 | 메모리 켠 상태 | 메모리 끈 상태 |
|---|---|---|
| 디자인 외주 끝냈는데 잔금을 안 줍니다 | **무호출** | `get_procedure` → 물품용역대금미수금 ✅ |
| 명도일이 다가오는데 어떻게 해야하나요 | **무호출** | `get_procedure` → 건물인도(명도)청구 ✅ |

알아챈 계기는 `"법원에서 뭐가 날아왔는데"`만 쳤는데 답변이 **"보정명령이면 일단 내용부터…"** 로 시작한 것입니다. 세 대화 전에 물었던 말이 새어 나왔습니다.

**메모리를 끄지 않으면 호출률이 실제보다 나쁘게 나옵니다.** 끄는 법과 나머지 주의점은 `audit/ROUTING_TEST_HOWTO.md`에 정리했습니다.

## 앞으로 어떻게 잴지

`audit/ROUTING_TEST_HOWTO.md`에 정리했습니다. 팀에서 라우팅을 건드릴 때 참고용입니다.

---

# 🔴 방법론의 구멍 — 우리는 원문으로 재는데, 실사용은 LLM 요약문이 들어갑니다

실사용 대화 하나를 받아 재현했습니다. 이 문서의 측정 방식 전체에 관한 문제라 따로 적습니다.

## 무슨 일이 있었나

사용자가 카카오에서 **"보정명령이 어마무시하네요"** 라고 했습니다. 호스트 LLM은 처음에 도구를 호출하지 않고 직접 답했고, 사용자가 "왜 법률절차길잡이 MCP 안 씀?"이라고 지적하자 그때 `triage`를 호출했습니다.

그 결과가 **렌터카 사고 — 자차면책금·수리비·휴차료 과다청구 대응** [소비자] 이었습니다.

## 같은 코드인데 결과가 다른 이유

```
search_topics("보정명령")                     → (0건)   정직함
search_topics("법원에서 보정명령을 받음")        → (0건)   정직함
triage("법원에서 보정명령을 받았고 요구사항이
       매우 많아 대응 방법을 알고 싶음")        → 렌터카사고분쟁   확신에 찬 오답
```

**모델이 달라서가 아닙니다.** 세 번째 문장을 로컬 빌드에 그대로 넣으면 똑같이 렌터카가 나옵니다. 차이는 **입력 문자열**입니다.

그리고 그 요약은 우리가 시킨 것입니다. `triage`의 `inputSchema` 설명:

> 사용자의 발화 원문을 그대로 넣지 말고, 이름·연락처 등 개인정보를 제외하고 **문제 유형 중심으로 요약해 전달하세요.**

개인정보를 빼려는 의도는 옳습니다. 다만 요약 과정에서 `대응`·`방법`·`요구사항` 같은 **범용어가 새로 붙고**, 그 말들이 노이즈 매칭을 일으킵니다.

## 점수 추적

| 주제 | 점수 | 근거 |
|---|---|---|
| **렌터카사고분쟁** | 6 | `"받았고"` +2 · **`"대응"` +4**(제목에 있음) |
| 허위조작정보피해 | 6 | `"매우"` +2 · **`"대응"` +4** |
| 소장받았을때 | 4 | `"법원에서"` +4 — 근거 1개라 게이트 탈락 |

`렌터카사고분쟁`의 제목이 *"렌터카 사고 — 자차면책금·수리비·휴차료 과다청구 **대응**"* 입니다. `"대응"`은 제목 매칭이라 +4를 받습니다. 정작 `보정명령`은 0점입니다 — 데이터에 없습니다.

**게이트(4점 + 근거 2개)가 노이즈 6점에 뚫렸습니다.** 원문이었다면 0건으로 정직하게 답했을 사안입니다.

## 이 문서의 측정치가 갖는 한계

2차 202개, 3차 195개 모두 **게시판 제목 원문**을 `search_topics`에 그대로 넣어 측정했습니다. 실사용 경로는 `사용자 발화 → 호스트 LLM 요약 → triage` 입니다. **중간 단계가 빠져 있습니다.**

따라서 이 문서의 숫자는 **낙관적일 가능성**이 있습니다. 요약이 범용어를 붙이면 0건이던 것이 오답으로 바뀔 수 있고, 위 사례가 정확히 그것입니다.

측정을 실사용에 맞추려면 제목을 그대로 넣지 말고, 호스트 LLM이 하듯 한 번 요약한 문장으로 돌려야 합니다. 이 PR에서는 하지 않았습니다.

## 함께 확인된 것 — `보정명령받았을때` 주제가 없습니다

법원에서 받은 서류에 대응하는 주제가 이미 세 개 있습니다.

```
소장받았을때 · 지급명령받았을때 · 약식명령받았을때
```

**보정명령만 없습니다.** `보정명령`이라는 말은 279개 주제 중 `민사소송소장`의 `비고`에 한 번 나오고, 그 필드는 점수 계산에 쓰이지 않습니다.

보정명령은 정해진 기간 안에 보정하지 않으면 소장이 각하될 수 있는 사안입니다. 기한이 걸린 절차이고, 이 서비스가 가장 잘해야 하는 종류입니다.

동의어만 넣어 `소장받았을때`로 연결하는 것은 "도착했는데 답이 없는" 상태를 만들 뿐이라 하지 않았습니다. **주제를 새로 쓰는 콘텐츠 작업이고 근거 조문 확인이 필요합니다(민사소송법 제254조 등).**

## 호스트 LLM이 제안한 개선안에 대한 의견

같은 대화에서 호스트 LLM이 개선안 6가지를 제시했습니다. 참고로 정리합니다.

| 제안 | 의견 |
|---|---|
| `보정명령받았을때` 주제 추가 | ✅ 타당. 위와 같은 이유 |
| 법원 절차 어휘를 도구 설명에 추가 | ✅ 싸고 정확 |
| triage에 confidence·abstain 도입 | ⚠️ **이미 있습니다.** 게이트(4점·근거 2개)가 그 역할이고 0건이 abstain입니다. 다만 이번에 노이즈 6점에 뚫렸습니다 |
| intent 분리 / 문서 분석 도구 / 새 출력 스키마 | ❌ 이번 실패의 원인은 데이터 한 줄과 노이즈 매칭입니다. 구조를 뜯을 근거로는 부족해 보입니다 |

---

# ⛔ 결정이 필요한 것 — 위기 표현이 채무 상담에 묻힙니다 (구현하지 않았습니다)

채무 카페(`ccrs5500`·`cafe20135031`) 실사용 제목 202개를 라우팅에 돌려보다 나왔습니다. **코드는 건드리지 않았습니다.** 혼자 판단할 사안이 아니라고 보아 근거만 정리합니다.

## 무슨 일이 일어나고 있나

`자살위기_도움받기` 주제는 **이미 있습니다.** 내용도 적절합니다 — 112·119, 자살예방 상담 **109**(24시간·비밀보장), 정신건강 위기상담 1577-0199, 자살시도자 치료비 지원.

그런데 **이 주제로 가는 동의어가 하나도 없습니다.** `"자살"`이라고 직접 써야만 걸립니다.

| 발화 | 현재 결과 |
|---|---|
| 자살 | 자살위기_도움받기 ✅ |
| 죽고 싶어요 | **(0건)** |
| 살고 싶지가 않아요 | **(0건)** ← 채무 카페에 실제로 올라온 제목입니다 |
| **빚 때문에 죽고 싶어요** | **개인회생** ⚠️ |

마지막 줄이 가장 나쁩니다. `빚` 신호가 이겨서 **위기 신호가 통째로 묻힙니다.** 채무 상담 서비스에서 드물지 않게 마주칠 조합입니다.

## 검토한 방법과, 그것을 넣지 않은 이유

주제 경쟁(`rankTopics`)에 위기 주제를 넣으면 채무 안내가 밀려나므로, **주제는 그대로 두고 안내문 앞에 도움처 한 줄을 얹는** 방식을 검토했습니다(`triage` 핸들러 6줄, 전화번호는 `PROCEDURES["자살위기_도움받기"].온라인접수`에서 읽음). 라우팅과 기존 테스트에는 영향이 없습니다.

그런데도 넣지 않았습니다. **다음 위험을 지시문으로는 보장할 수 없기 때문입니다.**

> 도움처를 앞세우면 호스트 LLM이 "전문가 상담을 받으세요"로 마무리하고 **원래 채무 안내를 하지 않을 수 있습니다.** 빚 문제는 그대로 남는데 안내는 사라집니다.

우리가 넣는 것은 강제가 아니라 지시문이고, 화면 카드에 번호가 박히는 구조도 아닙니다. **"둘 다 나간다"를 보장할 수단이 지금 구조에는 없습니다.** 이 실패는 절대 일어나서는 안 되는 종류라고 판단해 구현을 보류했습니다.

## 함께 측정한 것 — 단어 목록 방식의 한계

위기 표현 판별을 정규식/단어 목록으로 하면 어디까지 잡히는지 실제로 재봤습니다.

| 잡아야 하는데 놓침 | 잡으면 안 되는데 걸림 |
|---|---|
| 살고 싶지가 않아요 | 죽고 싶을 만큼 화나요 |
| 그만 살고 싶어요 | 자살골 넣었어요 |
| 사라지고 싶어요 | |
| 다 끝내고 싶어요 | |

`살고 싶지"가" 않아요`는 조사 하나 때문에 빠집니다. **완곡한 표현일수록 위험한데, 단어 목록은 그쪽을 가장 못 잡습니다.** 목록을 늘려도 끝이 없습니다.

## 결정이 필요한 지점

1. **다루는가** — 지금은 0건이라 우리 쪽에서 아무것도 나가지 않습니다(호스트 LLM 자체 대응에 전적으로 의존). 그대로 두는 것도 선택지입니다.
2. **다룬다면, "둘 다 나간다"를 어떻게 보장하는가** — 위젯 카드에 도움처를 함께 싣는 방법 등, 지시문에 의존하지 않는 수단이 필요해 보입니다.
3. **한계를 어디에 적는가** — 넣기로 한다면 "완곡한 표현은 잡지 못하며 최종 안전망이 아니다"를 문서에 남겨야 합니다. 놓쳐도 헛나가도 로그에 남지 않고 아무도 신고하지 않는 종류라, 넣었다는 사실만으로 안심하게 되는 것이 가장 큰 위험입니다.

---

# 참고

- 판단은 `search_topics` 1순위 기준입니다. `triage`도 같은 `rankTopics`를 씁니다.
- 기대 주제를 넉넉하게 잡았습니다. 좁게 잡으면 통과율이 더 내려갑니다.
- 프리뷰 실대화 4건 중 3건(짧은 질문)은 **도구가 아예 호출되지 않았습니다.** 라우팅과는 별개 사안이라 이 문서에서는 다루지 않았습니다.
- 원인 3개 중 **①만 고쳤습니다.** ②(`hay`에 근거법 전문)·③(어절 조사 매칭)은 손대지 않았습니다 — ①만으로 18개가 전부 통과해 필요가 없어졌습니다. 다른 계열에서 오라우팅이 다시 나오면 그때 봐야 합니다.
